info:
  name: Create project
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/projects"
  body:
    type: json
    data: |-
      {
        "name": "Oh hi Mark !",
        "description": "I have no idea how to describe this project..."
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
