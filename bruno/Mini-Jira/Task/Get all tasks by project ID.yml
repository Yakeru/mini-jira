info:
  name: Get all tasks by project ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/projects/{{project_id}}/tasks"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
