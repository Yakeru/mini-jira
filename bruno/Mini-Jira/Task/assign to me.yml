info:
  name: assign to me
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/projects/{projectId}/tasks/assigned-to-me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
